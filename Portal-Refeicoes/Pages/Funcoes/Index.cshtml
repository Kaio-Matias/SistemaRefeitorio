@page
@model Portal_Refeicoes.Pages.Funcoes.IndexModel
@{
    ViewData["Title"] = "Funções";
}

<h1>Gestão de Funções</h1>

<p>
    <a asp-page="Create" class="btn btn-primary">Nova Função</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nome</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Funcoes)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Nome</td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Editar</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>